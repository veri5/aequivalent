const state = {
  profile: { 
    name: 'Acme',
    logo: 'acme_logo.png', 
    fallbackLogo: 'logo_fallback.png',
  }
}

const mutations = {}

const actions = {}

const getters = {}

export default {
  namespaced: true,
  state,
  mutations,
  actions,
  getters,
}