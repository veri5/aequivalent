const state = {
  profile: { 
    name: 'Verifive',
    logo: 'verifive_logo.png', 
    fallbackLogo: 'logo_fallback.png',
  }
}

const mutations = {}

const actions = {}

const getters = {}

export default {
  namespaced: true,
  state,
  mutations,
  actions,
  getters,
}